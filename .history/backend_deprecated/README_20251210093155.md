Pet Supply Shops - Node.js + MySQL backend (deprecated copy)

This folder is a deprecated copy of the original backend moved from `backend/` to avoid affecting the active workspace.

See `backend/` for the active copy (if present) or use this folder to recover files if needed.
