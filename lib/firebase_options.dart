// Placeholder `firebase_options.dart` for the FlutterFire CLI output.
//
// To enable Firebase for web and other platforms, run:
//   dart pub global activate flutterfire_cli
//   flutterfire configure
//
// That will generate a proper `DefaultFirebaseOptions` implementation
// with `currentPlatform` returning a `FirebaseOptions` object for the
// current platform. Replace this file with the generated one.

class DefaultFirebaseOptions {
  // When not configured this returns `null`. The app checks for null
  // and will show a helpful message on web if config is missing.
  static const currentPlatform = null;
}
